# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Cargo.lock IS committed: Askeladd ships binaries and a demo, and the lock
# pins a known-good dependency graph (the registry no longer resolves cleanly
# against the pinned toolchain without it).

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb


# Added by cargo

/target

# Ignore macos specific files
.DS_Store

# Ignore .env files
.env